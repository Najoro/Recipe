{% extends 'base.html.twig' %}

{% block title %}
	Recette
{% endblock %}

{% block body %}

	<div class="container mt-2">
		<h1>Liste des Recettes</h1>
		<a class="btn btn-primary mb-2" href={{path('recipe_created')}}>Nouvelle recette</a>
		<table class="table table-striped table-inverse table-responsive">
			<thead class="thead-inverse">
				<tr>
					<th>Titre</th>
					<th>Duree</th>
					<th>Action</th>
				</tr>
				</thead>
				<tbody>
        {% for recipe in recipes %}

					<tr>
						<td scope="row"><a href={{ path('recette_show', {id : recipe.id , slug : recipe.slug} ) }}>{{recipe.title}}</a></td>
						<td>{{recipe.durations}}</td>
						<td>
							<a class="btn btn-warning btn-sm" href="">Editer</a>
							<a class="btn btn-danger btn-sm" href="">Supprimer</a>
						</td>
					</tr>
        {% endfor %}

				</tbody>
		</table>
	</div>

{% endblock %}
